# 多智能体脚本（Agents）——接口与契约集中定义

> 本文档是 AI 编码时的**权威接口参考**。所有 TypeScript 类型定义可直接用于实际代码。
> 接口定义以本文档为准，功能卡中的描述若有冲突以此为准。

---

## 一、跨脚本共享契约

> ⚠️ 本章内容与 VarUpdate 侧的接口与契约集中定义文档**完全相同**。任何修改必须同步更新两份文档。

### 1.1 事件协议

#### 事件名常量

```typescript
/**
 * 跨脚本事件名常量
 * 所有事件均通过酒馆助手事件 API 传输
 */
const EVENTS = {
  // ─── Agents 广播 ───
  PIPELINE_STARTED:  'agents:pipeline_started',
  STEP_STARTED:      'agents:step_started',
  MESSAGE_COMPLETE:  'agents:message_complete',
  STEP_RETRY:        'agents:step_retry',
  STEP_FAILED:       'agents:step_failed',
  PIPELINE_ENDED:    'agents:pipeline_ended',

  // ─── VarUpdate 广播 ───
  VAR_INITIALIZED:   'varupdate:initialized',
  VAR_UPDATED:       'varupdate:updated',
  VAR_UPDATE_FAILED: 'varupdate:update_failed',
  VAR_SCHEMA_READY:  'varupdate:schema_ready',

  // ─── 跨脚本协作 ───
  RETRY_REQUESTED:   'varupdate:retry_requested',
} as const;
```

#### 事件总览

| 事件名 | 方向 | 触发时机 |
|--------|------|---------|
| `agents:pipeline_started` | Agents → 外部 | 管道开始执行 |
| `agents:step_started` | Agents → 外部 | 单个 Agent 步骤开始 |
| `agents:message_complete` | **Agents → VarUpdate** | 单步生成完成（所有写入方式均触发） |
| `agents:step_retry` | Agents → 外部 | 单步触发重试 |
| `agents:step_failed` | Agents → 外部 | 单步彻底失败 |
| `agents:pipeline_ended` | Agents → 外部 | 管道执行结束 |
| `varupdate:initialized` | VarUpdate → 外部 | 变量初始化完成 |
| `varupdate:updated` | **VarUpdate → Agents** | 变量更新成功（重试联动的成功信号） |
| `varupdate:update_failed` | **VarUpdate → Agents** | 变量更新失败（重试联动的失败信号） |
| `varupdate:schema_ready` | VarUpdate → 外部 | Schema 编译完成 |
| `varupdate:retry_requested` | **Agents → VarUpdate** | 通知 VarUpdate 回退变量状态 |

> **加粗**的行是两个脚本之间有直接依赖关系的关键事件。

**VarUpdate 与 Agents 的耦合边界**：VarUpdate 在脚本内部使用「容错阈值」等规则决定单次解析是否记为失败，并在负载中附带 `discardedCount` 等字段供排查。**Agents 只需**根据 `varupdate:updated`（视为本步变量侧通过，可继续流水线）与 `varupdate:update_failed`（视为失败，可按约定广播 `varupdate:retry_requested` 等）做二分决策，**无需**读取或与 VarUpdate 的全局「容错阈值」配置保持同步。

#### 核心交互时序

```text
用户点击发送
  │
  ├─ Agents 广播 PIPELINE_STARTED
  │
  ├─ 遍历启用的 Agent:
  │   │
  │   ├─ 广播 STEP_STARTED
  │   ├─ 组装提示词 → API 调用 → 后处理 → 写入
  │   ├─ 广播 MESSAGE_COMPLETE ──────→ VarUpdate 监听
  │   │                                      │
  │   │                        ┌──────────────┴──────────────┐
  │   │                        ▼                             ▼
  │   │                   含变量标签                     不含变量标签
  │   │                        │                             │
  │   │                   执行解析更新                     忽略
  │   │                        │
  │   │              ┌─────────┴─────────┐
  │   │              ▼                   ▼
  │   │       广播 VAR_UPDATED    广播 VAR_UPDATE_FAILED
  │   │              │                   │
  │   │              ▼                   ▼
  │   │       Agents: 通过        Agents: 广播 RETRY_REQUESTED
  │   │                                  │
  │   │                           VarUpdate: 回退变量
  │   │                                  │
  │   │                           Agents: 重试本步骤
  │   │                           广播 STEP_RETRY
  │   │
  │   └─ 步骤完成 or 失败（广播 STEP_FAILED）
  │
  └─ 广播 PIPELINE_ENDED
```

#### 事件负载类型定义

```typescript
// ═══════════════════════════════════════════════════════════
//  Agents 广播的事件负载
// ═══════════════════════════════════════════════════════════

/** agents:pipeline_started */
interface PipelineStartedPayload {
  /** 当前方案名称 */
  schemeName: string;
  /** 启用的智能体数量 */
  agentCount: number;
}

/** agents:step_started */
interface StepStartedPayload {
  /** 智能体唯一 ID */
  agentId: string;
  /** 智能体标签名 */
  agentLabel: string;
  /** 步骤序号（从 0 开始） */
  stepIndex: number;
  /** 步骤字母标识（"A", "B", "C"...） */
  stepLetter: string;
}

/**
 * agents:message_complete
 *
 * 最关键的跨脚本事件。VarUpdate 通过此事件接收 Agents 流水线
 * 产生的消息内容，即使「仅解析」模式也会广播。
 */
interface MessageCompletePayload {
  /** 智能体唯一 ID */
  agentId: string;
  /** 智能体标签名 */
  agentLabel: string;
  /** 步骤序号 */
  stepIndex: number;
  /** 生成的完整消息文本（经后处理） */
  content: string;
  /** 推理/思维链内容（若配置传回） */
  reasoning?: string;
  /**
   * chat[] 中的消息索引。
   * - 写入聊天记录的模式：有值
   * - 「仅解析」模式：undefined
   */
  messageIndex?: number;
  /** 写入方式 */
  writeMode: WriteMode;
}

/** agents:step_retry */
interface StepRetryPayload {
  agentId: string;
  agentLabel: string;
  stepIndex: number;
  /** 当前重试次数（第几次重试） */
  retryCount: number;
  /** 最大重试次数 */
  maxRetries: number;
  /** 重试原因 */
  reason: RetryReason;
}

/** agents:step_failed */
interface StepFailedPayload {
  agentId: string;
  agentLabel: string;
  stepIndex: number;
  /** 人类可读的错误描述 */
  error: string;
  /** 失败类型 */
  failureType: FailureType;
}

/** agents:pipeline_ended */
interface PipelineEndedPayload {
  /** 管道是否成功完成（所有步骤均通过） */
  success: boolean;
  /** 已完成的步骤数 */
  stepsCompleted: number;
  /** 总步骤数 */
  totalSteps: number;
  /** 是否被用户手动中止 */
  aborted: boolean;
}

// ═══════════════════════════════════════════════════════════
//  VarUpdate 广播的事件负载
// ═══════════════════════════════════════════════════════════

/** varupdate:initialized */
interface VarInitializedPayload {
  /** 初始化数据写入的消息索引 */
  messageIndex: number;
  /** 初始化后的完整变量状态 */
  data: Record<string, any>;
}

/** varupdate:updated */
interface VarUpdatedPayload {
  /** 触发更新的消息索引 */
  messageIndex: number;
  /** 成功执行的指令数 */
  appliedCount: number;
  /** 被丢弃的指令数 */
  discardedCount: number;
  /** 变更日志（路径 → "旧值 → 新值"） */
  log: Record<string, string>;
}

/** varupdate:update_failed */
interface VarUpdateFailedPayload {
  /** 触发更新的消息索引 */
  messageIndex: number;
  /** 失败原因描述 */
  reason: string;
  /** 被丢弃的指令数 */
  discardedCount: number;
}

/** varupdate:schema_ready */
interface SchemaReadyPayload {
  /** $defs 中定义的结构体名称列表 */
  defNames: string[];
}

// ═══════════════════════════════════════════════════════════
//  跨脚本协作事件负载
// ═══════════════════════════════════════════════════════════

/**
 * varupdate:retry_requested
 *
 * 由 Agents 广播，VarUpdate 监听。
 * 语义：当前步骤的输出不合格，即将重新生成。
 * VarUpdate 收到后将本次解析写入的变量全部撤销。
 */
interface RetryRequestedPayload {
  /** 需要回退变量的消息索引 */
  messageIndex: number;
}
```

### 1.2 共享枚举与数据格式

#### 枚举类型

```typescript
/** 通知等级（两个脚本使用完全相同的五级体系） */
type NotifyLevel = 'debug' | 'always' | 'notice' | 'error' | 'silence';

/** 通知等级数值映射（用于比较：当前等级 >= 消息等级时才输出） */
const NOTIFY_LEVEL_VALUE: Record<NotifyLevel, number> = {
  debug: 0,
  always: 1,
  notice: 2,
  error: 3,
  silence: 4,
};

/** 写入方式（Agents 后处理器使用，事件负载中传递） */
type WriteMode =
  | 'parse_only'   // 仅解析，不写入 chat[]
  | 'user'         // 独立楼层（User 角色）
  | 'assistant'    // 独立楼层（Assistant 角色）
  | 'append'       // 追加到最新楼层末尾
  | 'overwrite';   // 覆写最新楼层

/** 重试原因 */
type RetryReason =
  | 'missing_end_marker'   // 结束标识符检查未通过
  | 'varupdate_failed'     // VarUpdate 反馈解析失败
  | 'timeout';             // 等待 VarUpdate 反馈超时

/** 步骤失败类型 */
type FailureType =
  | 'max_retries_exceeded' // 超过最大重试次数
  | 'api_error'            // API 调用错误
  | 'user_aborted';        // 用户手动中止

/** 全局标签策略（Agents 标签系统使用） */
type GlobalPolicy =
  | 'include'    // 无标签条目默认包含
  | 'exclude';   // 无标签条目默认排除
```

#### 变量路径规范

```typescript
/**
 * 变量路径格式规范
 *
 * 使用 `/` 作为层级分隔符，以下场景共用同一套规范：
 * - VarUpdate Update 指令路径    → /角色/HP
 * - VarUpdate refer() 引用       → refer(角色/HP上限)
 * - VarUpdate 插值宏             → {{message/data/角色/HP}}
 * - Agents 变量条件标签           → ["角色/HP" >= 60]
 *
 * 规则：
 * - 根路径 ""              → 返回根对象本身
 * - "角色/HP"              → data.角色.HP
 * - "背包/0/名称"          → data.背包[0].名称（纯数字段视为数组索引）
 * - "背包/-"               → 数组末尾追加（仅 Update insert 使用）
 * - 路径中任何一层不存在时：
 *     读取 → 返回 undefined（不报错）
 *     写入 → 由调用方决定行为
 */
type VariablePath = string;

/**
 * 路径工具函数签名（两个脚本各自实现于 shared/path-utils.ts）
 */
declare function parsePath(path: string): string[];
declare function getValueByPath(data: any, path: string): any;
declare function setValueByPath(data: any, path: string, value: any): void;
declare function deleteByPath(data: any, path: string): boolean;
```

### 1.3 共享行为约定

#### 通配符匹配规则

```typescript
/**
 * 通配符匹配规则（由公共库 wildcard-match 实现）
 *
 * 使用场景：
 * - Agents 标签条件中的字符串比较（面向用户 B-3b）
 * - VarUpdate Schema 的 $enum 通配符匹配（面向用户 A-3）
 * - VarUpdate Schema 的 $key_rule 通配符匹配
 *
 * 个数规则：
 * - 1~2 个 * → 每个 * 严格匹配恰好 1 个字符
 * - 3+ 个 *  → 匹配任意数量字符（0 到无穷）
 *
 * 位置规则：
 * - 通配符在哪侧，该侧允许有额外内容
 * - 无通配符的一侧必须是字符串边界
 *
 * 示例：
 * - "***开心***" → 任意位置包含"开心"
 * - "***开心"    → 以"开心"结尾
 * - "开心***"    → 以"开心"开头
 * - "*开心*"     → 前后各恰好 1 字符
 * - "**民主主义" → 前面恰好 2 字符
 */
declare function wildcardMatch(pattern: string, text: string): boolean;
```

#### 变量数据读取位置

```typescript
/**
 * 变量数据读取约定
 *
 * Agents 标签条件和 VarUpdate 插值宏均从同一位置读取变量值：
 * - 作用域：message 层
 * - 字段：data
 * - 消息索引：最新一条消息（chat.length - 1）
 *
 * 等价调用：
 *   getVariables({ type: 'message', messageId: latestMessageIndex })
 *
 * 返回值结构示例：
 *   {
 *     "角色": { "HP": 80, "MP": 50 },
 *     "背包": [{ "名称": "铁剑", "数量": 1 }]
 *   }
 */
```

#### 事件总线接口规范

```typescript
/**
 * 事件总线统一接口
 *
 * 两个脚本的事件总线模块（Agents 模块7 / VarUpdate 模块7）
 * 具有完全相同的接口签名和实现模式。
 */
interface EventBus {
  /** 广播事件 */
  emit(eventName: string, payload: any): Promise<void>;
  /**
   * 持续监听事件
   * @returns 包含 stop 方法的对象（与酒馆助手 eventOn 返回值一致）
   */
  on(eventName: string, handler: (payload: any) => void): { stop: () => void };
  /**
   * 一次性监听事件（触发后自动注销）
   * @returns 包含 stop 方法的对象（与酒馆助手 eventOnce 返回值一致）
   */
  once(eventName: string, handler: (payload: any) => void): { stop: () => void };
  /** 注销本脚本注册的所有事件监听器（脚本卸载时调用） */
  removeAll(): void;
}

/**
 * 底层实现：委托酒馆助手 API
 *
 * emit      → TavernHelper._bind._eventEmit.call(window, eventName, payload)
 * on        → TavernHelper._bind._eventOn.call(window, eventName, handler)
 * once      → TavernHelper._bind._eventOnce.call(window, eventName, handler)
 * removeAll → TavernHelper._bind._eventClearAll.call(window)
 *
 * 命名空间：
 * - Agents 事件使用 "agents:" 前缀
 * - VarUpdate 事件使用 "varupdate:" 前缀
 */
```

#### VarUpdate 双通道消息处理

```typescript
/**
 * VarUpdate 通过两个独立通道接收消息：
 *
 * 通道 A：agents:message_complete 事件（优先）
 *   → 由 Agents 广播，从事件负载 content 字段读取消息
 *   →「仅解析」模式下 messageIndex 为 undefined，使用 content 直接处理
 *
 * 通道 B：酒馆原生 MESSAGE_RECEIVED 事件（备用）
 *   → 从 chat[messageIndex].mes 读取消息文本
 *   → 仅在 Agents 未加载或未运行时作为独立消息来源
 *
 * 互斥判定（避免同一条消息被两个通道重复处理）：
 *
 * 主方案（事件标志位）：
 *   VarUpdate 维护内部标志 agentsActive: boolean
 *   - 监听 agents:pipeline_started → agentsActive = true
 *   - 监听 agents:pipeline_ended   → agentsActive = false
 *   - 通道 B 触发时，若 agentsActive === true → 跳过
 *
 * 备用方案（时间窗口）：
 *   通道 B 触发时，检查最近 N 毫秒内是否收到过
 *   agents:message_complete 事件。若有，说明该消息已由通道 A 处理，跳过。
 *   此方案覆盖标志位因异常未及时更新的极端情况。
 */
```

---

## 二、外部依赖契约

> ⚠️ 本章内容与 VarUpdate 侧的接口与契约集中定义文档**完全相同**。任何修改必须同步更新两份文档。
> 本章记录两个脚本共同依赖的外部 API。这些 API 没有版本保证，酒馆/酒馆助手更新时可能变更。

### 2.1 酒馆助手（TavernHelper）API

脚本运行在酒馆助手的 iframe 沙箱中，以下函数为全局可用。

#### 事件系统

```typescript
/**
 * 监听事件。listener 已在监听时调用无效果。
 * 脚本/iframe 关闭时监听自动卸载。
 */
declare function eventOn<T extends EventType>(
  event_type: T,
  listener: ListenerType[T]
): { stop: () => void };

/**
 * 一次性监听（触发后自动注销）
 */
declare function eventOnce<T extends EventType>(
  event_type: T,
  listener: ListenerType[T]
): { stop: () => void };

/**
 * 广播事件。返回 Promise，await 可等待所有 listener 执行完毕。
 * 可携带任意数量的数据参数。
 */
declare function eventEmit<T extends EventType>(
  event_type: T,
  ...data: Parameters<ListenerType[T]>
): Promise<void>;

/** 取消指定 listener 对指定事件的监听 */
declare function eventRemoveListener<T extends EventType>(
  event_type: T,
  listener: ListenerType[T]
): void;

/** 取消本 iframe 中对所有事件的所有监听 */
declare function eventClearAll(): void;

/** 取消本 iframe 中对指定事件的所有监听 */
declare function eventClearEvent(event_type: EventType): void;

/** 事件类型可以是 iframe 事件、酒馆事件或自定义字符串 */
type EventType = IframeEventType | TavernEventType | string;

/** 自定义事件的 listener 签名为 (...args: any) => any */
```

#### 变量系统

```typescript
type VariableOption =
  | { type: 'global' }
  | { type: 'chat' }
  | { type: 'character' }
  | { type: 'preset' }
  | { type: 'message'; message_id?: number | 'latest' }
  | { type: 'script'; script_id?: string }
  | { type: 'extension'; extension_id: string };

/**
 * 读取指定作用域的变量（返回深拷贝）
 * 默认 option 为 { type: 'chat' }
 */
declare function getVariables(
  option?: VariableOption
): Record<string, any>;

/**
 * 全量替换指定作用域的变量
 * 默认 option 为 { type: 'chat' }
 */
declare function replaceVariables(
  variables: Record<string, any>,
  option?: VariableOption
): void;

/**
 * 用 updater 函数读取→修改→写回变量
 * updater 可返回 Promise
 */
declare function updateVariablesWith(
  updater: (variables: Record<string, any>) => Record<string, any>,
  option?: VariableOption
): Record<string, any>;

/**
 * 合并写入变量（已有键保留，新键插入）
 */
declare function insertOrAssignVariables(
  variables: Record<string, any>,
  option?: VariableOption
): Record<string, any>;

/**
 * 注册变量 Schema（Zod 对象），供变量管理器 UI 使用
 */
declare function registerVariableSchema(
  schema: z.ZodType<any>,
  option: { type: 'global' | 'preset' | 'character' | 'chat' | 'message' }
): void;

/**
 * 获取合并后的全层变量表
 * 脚本 iframe 中：global → character → script → chat → message[0..latest]
 */
declare function getAllVariables(): Record<string, any>;

/**
 * 变量存储位置映射：
 * - global    → extension_settings.variables.global
 * - chat      → chat_metadata.variables
 * - message   → chat[message_id].variables[swipe_id]
 * - character → characterSettings.variables
 * - preset    → presetSettings.variables
 * - script    → scriptIframeRuntimes[script_id].data
 */
```

> **我们的脚本使用 `global`、`chat`、`message` 三个 scope**，不使用 `character`、`preset`、`script`。

#### 宏注册

```typescript
interface MacroLikeContext {
  message_id?: number;
  role?: 'user' | 'assistant' | 'system';
}

/**
 * 注册自定义插值宏
 * @param regex 匹配宏的正则表达式
 * @param replace 替换函数，首参为上下文（含 message_id）
 * @returns 包含 unregister 方法的对象
 */
declare function registerMacroLike(
  regex: RegExp,
  replace: (context: MacroLikeContext, substring: string, ...args: any[]) => string
): { unregister: () => void };
```

#### 按钮注册

```typescript
interface ScriptButton {
  name: string;
  visible: boolean;
}

/** 获取按钮对应的事件类型字符串 */
declare function getButtonEvent(button_name: string): string;

/** 追加不存在的按钮到脚本按钮列表末尾（不重复添加同名按钮） */
declare function appendInexistentScriptButtons(buttons: ScriptButton[]): void;

/** 完全替换脚本按钮列表 */
declare function replaceScriptButtons(buttons: ScriptButton[]): void;
```

### 2.2 酒馆本体关键类型

以下类型从 SillyTavern 本体导出，在 iframe 中通过 `SillyTavern` 全局对象或直接类型引用访问。

#### ChatMessage（聊天消息）

```typescript
/** chat[] 数组中每条消息的数据结构 */
interface ChatMessage {
  name: string;
  /** true = user 消息, false = assistant/narrator 消息 */
  is_user: boolean;
  /** true = 隐藏消息（不发送给 LLM） */
  is_system: boolean;
  /** 消息原始文本 */
  mes: string;
  /** 当前 swipe 索引 */
  swipe_id?: number;
  /** 所有 swipe 的文本数组 */
  swipes?: string[];
  /** swipe 元信息 */
  swipe_info?: Record<string, any>[];
  extra?: Record<string, any>;
  /**
   * 变量存储：按 swipe 索引独立存储
   * variables[swipe_id] → 该 swipe 的变量数据
   */
  variables?: Record<string, any>[] | { [swipe_id: number]: Record<string, any> };
}
```

#### FlattenedWorldInfoEntry（世界书条目）

```typescript
/** 世界书扫描后的扁平化条目结构 */
interface FlattenedWorldInfoEntry {
  uid: number;
  displayIndex: number;
  /** 条目备注/标题 — 标签系统读取此字段 */
  comment?: string;
  /** 条目是否禁用 */
  disable: boolean;
  /** 永久激活 */
  constant: boolean;
  selective: boolean;
  /** 主关键词 */
  key: string[];
  /** 0: and_any, 1: not_all, 2: not_any, 3: and_all */
  selectiveLogic: 0 | 1 | 2 | 3;
  /** 次要关键词 */
  keysecondary: string[];
  scanDepth: number | null;
  vectorized: boolean;
  /** 插入位置：0-6 对应不同位置 */
  position: 0 | 1 | 2 | 3 | 4 | 5 | 6;
  /** 角色：0=system, 1=user, 2=assistant */
  role: 0 | 1 | 2 | null;
  depth: number;
  order: number;
  /** 条目内容文本 */
  content: string;
  useProbability: boolean;
  probability: number;
  excludeRecursion: boolean;
  preventRecursion: boolean;
  delayUntilRecursion: boolean | number;
  sticky: number | null;
  cooldown: number | null;
  delay: number | null;
  extra?: Record<string, any>;
}
```

#### RegexScriptData（正则脚本）

```typescript
/** 正则脚本数据结构 */
interface RegexScriptData {
  id: string;
  /** 脚本名称 — 标签系统读取此字段 */
  scriptName: string;
  findRegex: string;
  replaceString: string;
  trimStrings: string[];
  /** 适用的消息来源类型 */
  placement: number[];
  disabled: boolean;
  /** 仅格式显示 */
  markdownOnly: boolean;
  /** 仅格式提示词 */
  promptOnly: boolean;
  /** 编辑时重新应用 */
  runOnEdit: boolean;
  substituteRegex: number;
  minDepth: number;
  maxDepth: number;
}
```

#### SendingMessage（发送给 API 的消息）

```typescript
/** 发送给 LLM API 的消息格式 */
interface SendingMessage {
  role: 'user' | 'assistant' | 'system';
  content:
    | string
    | Array<
        | { type: 'text'; text: string }
        | { type: 'image_url'; image_url: { url: string; detail: 'auto' | 'low' | 'high' } }
      >;
}
```

#### 关键酒馆事件

```typescript
/**
 * 本项目使用到的酒馆原生事件（通过 tavern_events 常量访问）
 */
const TAVERN_EVENTS_USED = {
  // ─── VarUpdate 监听 ───
  MESSAGE_RECEIVED:       'message_received',     // (message_id: number, type: string) => void
  MESSAGE_SWIPED:         'message_swiped',       // (message_id: number) => void
  MESSAGE_EDITED:         'message_edited',       // (message_id: number) => void
  MESSAGE_DELETED:        'message_deleted',       // (newChatLength: number) => void —— SillyTavern 传入删除后的 chat.length
  CHAT_CHANGED:           'chat_id_changed',      // (chat_file_name: string) => void

  // ─── Agents 监听 ───
  GENERATION_STARTED:     'generation_started',    // (type, option, dry_run) => void
  GENERATION_ENDED:       'generation_ended',      // (message_id: number) => void

  // ─── Agents 世界书标签过滤 ───
  WORLDINFO_SCAN_DONE:    'worldinfo_scan_done',   // (event_data) => void
  // event_data.activated.entries: Map<string, FlattenedWorldInfoEntry>

  // ─── 渲染 ───
  CHARACTER_MESSAGE_RENDERED: 'character_message_rendered', // (message_id: number, type: string) => void
  USER_MESSAGE_RENDERED:      'user_message_rendered',      // (message_id: number) => void
} as const;
```

#### SillyTavern 全局接口（选用部分）

```typescript
/**
 * 通过 SillyTavern 全局对象访问的常用方法
 * 完整接口见酒馆源码 st-context.js
 */
declare const SillyTavern: {
  readonly chat: ChatMessage[];
  readonly chatMetadata: Record<string, any>;  // = chat_metadata
  readonly extensionSettings: Record<string, any>;  // = extension_settings
  readonly name1: string;   // 用户名
  readonly name2: string;   // 角色名
  readonly characterId: string;
  readonly maxContext: number;

  // 消息操作
  readonly addOneMessage: (mes: ChatMessage, options?: {
    insertAfter?: number;
    insertBefore?: number;
    forceId?: number;
  }) => JQuery<HTMLElement>;
  readonly deleteLastMessage: () => Promise<void>;

  // 生成
  readonly generate: Function;
  readonly stopGeneration: () => boolean;

  // Token 计数
  readonly getTokenCountAsync: (text: string, padding?: number) => Promise<number>;

  // 设置保存
  readonly saveSettingsDebounced: () => Promise<void>;
  readonly saveChat: () => Promise<void>;

  // 事件（等价于直接使用全局 eventOn/eventEmit 等）
  readonly eventSource: {
    on: typeof eventOn;
    emit: typeof eventEmit;
    once: typeof eventOnce;
    removeListener: typeof eventRemoveListener;
  };
  readonly eventTypes: typeof tavern_events;
};
```

### 2.3 公共库 API

#### label-parser（标签解析器）

```typescript
/**
 * 从文本中解析所有 [...] 标签
 * 由 Agents 模块1 调用
 *
 * @param text 包含标签的文本（预设标题/世界书 comment/正则 scriptName）
 * @returns 解析出的标签数组
 */
declare function parseLabels(text: string): ParsedLabel[];

interface ParsedLabel {
  /** 标签原始文本（含定界符） */
  raw: string;
  /** 标签内容（不含定界符） */
  content: string;
  /** 标签在原文中的起始位置 */
  startIndex: number;
  /** 标签在原文中的结束位置 */
  endIndex: number;
}
```

#### wildcard-match（通配符匹配）

```typescript
/**
 * 通配符模式匹配
 * 规则见第一章 1.3 共享行为约定
 *
 * 由 Agents 模块1 和 VarUpdate 模块2 调用
 */
declare function wildcardMatch(pattern: string, text: string): boolean;
```

#### flexible-json-patch（容错 JSON Patch 解析）

```typescript
/**
 * 宽松解析 JSON Patch 指令文本
 * 由 VarUpdate 模块3 第一层管道调用
 *
 * 职责：文本清洗 → 引号修正 → 宽松 JSON 解析 → 语义规范化
 * 详细规则见 VarUpdate 基础设施功能卡 四·模块3
 */
declare function parseFlexibleJsonPatch(rawText: string): PatchInstruction[];

interface PatchInstruction {
  op: 'replace' | 'insert' | 'delete';
  path: string;
  value?: any;
}
```

#### schema-to-zod（Schema → Zod 编译器）

```typescript
/**
 * 将声明式 Schema 编译为 Zod 校验对象
 * 由 VarUpdate 模块2 核心调用
 *
 * 详细规则见 VarUpdate 基础设施功能卡 三·模块2
 */
declare function compileSchemaToZod(
  schemaData: Record<string, any>,
  defRegistry?: Map<string, z.ZodType>
): z.ZodType<any>;
```

#### promptal-yaml（PromptalYAML 序列化器）

```typescript
/**
 * 将 JavaScript 对象序列化为 PromptalYAML 格式文本
 * 由 VarUpdate 模块6 调用
 *
 * 详细规则见 VarUpdate 基础设施功能卡 七·模块6
 */
declare function serializeToPromptalYAML(
  value: any,
  indentLevel?: number
): string;
```

---

## 三、模块公开接口

> 本章为 Agents 独有内容。每个模块定义导出函数签名、输入输出类型、上下游依赖。

### 模块 1：标签解析引擎

**文件**：`src/label-engine.ts`
**上游依赖**：公共库 `label-parser`、`wildcard-match`
**下游消费方**：模块 2（管道引擎）

```typescript
interface LabelContext {
  /** 当前 Agent 的标签名 */
  agentLabel: string;
  /** 全局策略 */
  globalPolicy: GlobalPolicy;
  /** 变量读取函数（读取 message 层 data 字段） */
  readVariable: (path: VariablePath) => any;
}

/**
 * 判定一个条目是否应在当前 Agent 步骤中注入
 *
 * 判定流程：
 * 1. parseLabels(text) 解析所有 [...] 标签
 * 2. 无标签 → 按 globalPolicy（include=true, exclude=false）
 * 3. [NonPrompts] → false, [All] → true
 * 4. 多标签 AND 组合：智能体匹配 + 变量条件
 * 5. 全部满足 → true，任一不满足 → false
 */
function shouldInject(text: string, ctx: LabelContext): boolean;
```

**变量条件运算符**：

| 运算符 | 语义 |
| --- | --- |
| `==` / `!=` | 宽松比较 |
| `===` / `!==` | 严格比较 |
| `>` `>=` `<` `<=` | 数值比较 |
| `?` / `!?` | 存在性检查 |
| `∋` / `!∋` | 数组包含 |
| `⊇` / `!⊇` | 对象键包含 |
| `#` + 比较 | 长度比较 |

---

### 模块 2：管道引擎

**文件**：`src/pipeline/pipeline.ts`
**上游依赖**：模块 1（标签），模块 6（配置读取）
**下游消费方**：主控制器、模块 10（dryRun）

```typescript
interface PipelineInput {
  /** 当前 Agent 配置 */
  agentConfig: AgentConfig;
  /** 标签上下文 */
  labelCtx: LabelContext;
}

interface PipelineOutput {
  /** 最终消息数组（按发送顺序排列） */
  messages: SendingMessage[];
  /** 预估 Token 数 */
  estimatedTokens: number;
  /** 调试信息 */
  debug: PipelineDebugInfo;
}

interface PipelineDebugInfo {
  /** 预设条目过滤统计 */
  preset: { total: number; kept: number; filtered: string[] };
  /** 世界书条目过滤统计 */
  worldInfo: { total: number; kept: number; filtered: string[] };
  /** 正则规则过滤统计 */
  regex: { total: number; kept: number; filtered: string[] };
  /** 聊天历史消息数 */
  chatMessages: number;
}

/**
 * 镜像酒馆原生管道，在每个环节插入标签过滤层
 */
function assemblePipeline(input: PipelineInput): Promise<PipelineOutput>;
```

**子模块接口**：

```typescript
// ─── 2a. 预设适配层 ───
// src/pipeline/preset-adapter.ts
// 依赖: 模块1
/** 过滤预设条目，返回仅保留匹配条目的有序数组 */
function filterPresetPrompts(ctx: LabelContext): PromptEntry[];

/** 单条预设条目（过滤后保留的） */
interface PromptEntry {
  /** 预设条目的标识符 */
  identifier: string;
  /** 角色（system / user / assistant） */
  role: string;
  /** 条目内容 */
  content: string;
  /** 在预设中的原始位置 */
  position: number;
}

// ─── 2b. 世界书适配层 ───
// src/pipeline/worldinfo-adapter.ts
// 依赖: 模块1
/** 从 WORLDINFO_SCAN_DONE 回调中过滤激活条目 */
function filterWorldInfoEntries(
  entries: Map<string, FlattenedWorldInfoEntry>,
  ctx: LabelContext
): Map<string, FlattenedWorldInfoEntry>;

// ─── 2c. 正则适配层 ───
// src/pipeline/regex-adapter.ts
// 依赖: 模块1
/** 按当前 Agent 过滤正则规则集 */
function filterRegexScripts(
  scripts: RegexScriptData[],
  ctx: LabelContext
): RegexScriptData[];

// ─── 2d. 聊天历史构建器 ───
// src/pipeline/chat-builder.ts
/** 截取并应用正则后的聊天历史 */
function buildChatHistory(
  historyDepth: number,
  filteredRegex: RegexScriptData[]
): SendingMessage[];
```

---

### 模块 3：API 调用层

**文件**：`src/api-caller.ts`
**上游依赖**：无（独立模块）
**下游消费方**：模块 5（重试循环中调用）、模块 10（连接测试/试生成）

```typescript
interface ApiRequest {
  url: string;
  apiKey: string;
  model: string;
  /** API 格式（openai / claude / google 等） */
  format: string;
  /** 模型参数（temperature / max_tokens 等） */
  params: Record<string, any>;
  /** 消息数组 */
  messages: SendingMessage[];
  /** 是否流式传输 */
  stream: boolean;
}

interface ApiResponse {
  /** 生成的文本内容 */
  content: string;
  /** 推理/思维链（若有） */
  reasoning?: string;
  /** Token 使用统计 */
  usage?: { prompt_tokens: number; completion_tokens: number };
}

/** 发送 API 请求 */
function sendRequest(
  request: ApiRequest,
  onChunk?: (chunk: string) => void
): Promise<ApiResponse>;

/** 获取模型列表（轻量探测） */
function fetchModelList(
  url: string,
  apiKey: string,
  format: string
): Promise<string[]>;
```

**API 参数映射表**（`data/api-params-map.json`）：

```typescript
/** 映射表结构 */
interface ApiParamsMap {
  [format: string]: {
    /** 通用参数（始终显示） */
    common: string[];
    /** 小众参数（展开后显示） */
    niche: string[];
    /** 默认值 */
    defaults: Record<string, any>;
  };
}
```

---

### 模块 4：后处理器

**文件**：`src/post-processor.ts`
**上游依赖**：无（纯函数）
**下游消费方**：模块 5（重试循环中调用）

```typescript
interface PostProcessConfig {
  /** 写入方式 */
  writeMode: WriteMode;
  /** 追加模式前置换行 */
  appendNewline: boolean;
  /** 裁剪开始标签前 */
  trimBefore: { enabled: boolean; tag: string };
  /** 裁剪结束标签后 */
  trimAfter: { enabled: boolean; tag: string };
}

interface PostProcessResult {
  /** 处理后的消息文本 */
  content: string;
  /** 处理后的推理文本（可能被丢弃） */
  reasoning?: string;
  /** 写入的 messageIndex（仅写入 chat[] 的模式有值） */
  messageIndex?: number;
}

/**
 * 后处理链（固定顺序）：
 * 1. 推理过程剥离（若未开启传回）
 * 2. 裁剪开始标签前
 * 3. 裁剪结束标签后
 */
function postProcess(
  raw: ApiResponse,
  config: PostProcessConfig,
  includeReasoning: boolean
): PostProcessResult;

/**
 * 按写入方式路由到对应输出
 * - parse_only: 不操作 chat[]
 * - user/assistant: addOneMessage()
 * - append: chat[last].mes += content
 * - overwrite: chat[last].mes = content
 */
function writeToChat(
  result: PostProcessResult,
  config: PostProcessConfig
): Promise<void>;
```

---

### 模块 5：重试控制器

**文件**：`src/retry-controller.ts`
**上游依赖**：模块 3（API）、模块 4（后处理）、模块 7（事件总线）
**下游消费方**：主控制器

```typescript
interface RetryConfig {
  /** 接收超时时长（秒） */
  timeout: number;
  /** 最大重试次数 */
  maxRetries: number;
  /** 重试间隔（秒） */
  retryInterval: number;
  /** 完整性检查（结束标识符） */
  endMarker: { enabled: boolean; marker: string };
  /** 变量解析联动 */
  varUpdateCheck: boolean;
}

interface StepExecutionInput {
  /** API 请求配置 */
  apiRequest: ApiRequest;
  /** 后处理配置 */
  postProcessConfig: PostProcessConfig;
  /** 重试配置 */
  retryConfig: RetryConfig;
  /** 是否传回推理过程 */
  includeReasoning: boolean;
  /** 流式渲染回调 */
  onChunk?: (chunk: string) => void;
}

interface StepExecutionResult {
  /** 最终后处理结果 */
  result: PostProcessResult;
  /** 实际重试次数 */
  retriesUsed: number;
}

/**
 * 执行单步生成（包含重试循环）
 *
 * 重试判定流程：
 * 1. 结束标识符检查（D-1）
 * 2. VarUpdate 联动等待（D-2）
 *    → 监听 varupdate:updated / varupdate:update_failed
 *    → 失败时广播 varupdate:retry_requested
 * 3. 超过最大重试次数 → 暂停管道，弹出用户提示
 */
function executeStep(input: StepExecutionInput): Promise<StepExecutionResult>;
```

---

### 模块 6：配置存储

**文件**：`src/config-store.ts`
**上游依赖**：酒馆本体 `extension_settings`
**下游消费方**：主控制器、模块 2、模块 9

```typescript
/** 完整存储结构见第四章 AgentsStorage */

/** 初始化存储连接 */
function initStore(): void;

// ─── 智能体 CRUD ───
function getAgent(id: string): AgentConfig | undefined;
function getAllAgents(): AgentConfig[];
function saveAgent(config: AgentConfig): void;
function deleteAgent(id: string): void;
function createAgentFromBuiltin(builtinId: string): AgentConfig;

// ─── 方案 CRUD ───
function getScheme(name: string): Scheme | undefined;
function getActiveScheme(): Scheme;
function setActiveScheme(name: string): void;
function saveScheme(scheme: Scheme): void;
function deleteScheme(name: string): void;

// ─── 全局设置 ───
function getGlobalSettings(): GlobalSettings;
function updateGlobalSettings(partial: Partial<GlobalSettings>): void;

// ─── 导入导出 ───
function exportAgent(id: string, stripApiKey?: boolean): string;
function importAgent(json: string): AgentConfig;
function exportScheme(name: string, includeAgents?: boolean): string;
function importScheme(json: string): Scheme;
```

---

### 模块 7：事件总线

**文件**：`src/event-bus.ts`
**上游依赖**：酒馆助手事件 API
**下游消费方**：所有其他模块

```typescript
/** 接口与第一章 EventBus 完全一致 */
function emit(eventName: string, payload: any): Promise<void>;
function on(eventName: string, handler: (payload: any) => void): { stop: () => void };
function once(eventName: string, handler: (payload: any) => void): { stop: () => void };
function removeAll(): void;

/** 事件名常量见第一章 EVENTS */
```

---

### 模块 8：通知系统

**文件**：`src/notify.ts`
**上游依赖**：宿主页面 `parent.toastr`
**下游消费方**：所有其他模块

```typescript
/**
 * 流水线事件通知（受通知等级控制）
 * 当 level >= 当前设置的通知等级时输出
 */
function notify(level: NotifyLevel, title: string, message: string): void;

/**
 * 用户操作反馈（silence 以外的等级始终弹窗）
 * silence 等级下不弹窗也不输出控制台
 */
function feedback(success: boolean, title: string, message: string): void;

/**
 * 控制台输出格式：
 * [Agents] 步骤 A 完成 (2.3s, 1280 chars)
 *
 * 四色分级：🔵debug / 🟢通过 / 🟠警告 / 🔴错误
 * 通过 CSS %c 格式化实现
 */
```

---

### 模块 9：UI 面板

**文件**：`src/ui/panel.ts`、`src/ui/agent-editor.ts`、`src/ui/status-button.ts`
**上游依赖**：模块 6（配置读取）、模块 10（辅助按钮）
**下游消费方**：用户交互

```typescript
// ─── 9a. 主面板 ───
/** 渲染脚本扩展面板（注入到酒馆助手容器） */
function renderPanel(): void;
/** 刷新面板数据（方案切换/配置变更后调用） */
function refreshPanel(): void;

// ─── 9b. 智能体配置浮窗 ───
/** 打开指定智能体的配置编辑器 */
function openAgentEditor(agentId: string): void;
/** 关闭编辑器（保存并关闭） */
function closeAgentEditor(): void;

// ─── 9c. 状态按钮 ───
/** 初始化自定义按钮（替换酒馆原生发送/中止按钮） */
function initStatusButton(): void;
/** 切换按钮状态 */
function setButtonState(state: 'idle' | 'running', stepLetter?: string): void;
/** 恢复酒馆原生按钮（卸载时调用） */
function restoreNativeButtons(): void;
```

---

### 模块 10：辅助工具

**文件**：`src/tools.ts`
**上游依赖**：模块 2（管道引擎）、模块 3（API 调用）
**下游消费方**：模块 9（UI 按钮点击）

```typescript
interface DryRunResult {
  /** 组装的完整消息列表 */
  messages: SendingMessage[];
  /** 预估 Token 数 */
  estimatedTokens: number;
  /** 过滤统计 */
  debug: PipelineDebugInfo;
}

interface TestResult {
  /** 是否成功 */
  success: boolean;
  /** 错误信息（失败时） */
  error?: string;
  /** 可用模型列表（成功时） */
  models?: string[];
}

interface GenerateResult {
  /** 是否成功 */
  success: boolean;
  /** 生成的文本 */
  content?: string;
  /** 错误信息 */
  error?: string;
}

/** Dry Run：完整提示词组装但不发送 API */
function dryRun(
  agentConfig: AgentConfig,
  labelCtx: LabelContext
): Promise<DryRunResult>;

/** 连接测试：轻量探测 API 连通性 */
function testConnection(
  url: string,
  apiKey: string,
  format: string
): Promise<TestResult>;

/** 试生成：使用测试提示词执行一次真实生成 */
function testGenerate(
  agentConfig: AgentConfig,
  testPrompt: string
): Promise<GenerateResult>;
```

---

## 四、内部数据结构

> 本章为 Agents 独有内容。定义配置存储 Schema 和所有复合数据类型。

### 4.1 存储根结构

```typescript
/**
 * Agents 完整存储结构
 * 智能体 + 方案: 持久化于 extension_settings.__userscripts?.['agents']
 * 全局设置: 持久化于酒馆助手变量系统 global 层，顶级键名 Agents_config
 */
interface AgentsStorage {
  /** 所有智能体（按 ID 索引） */
  agents: Record<string, AgentConfig>;
  /** 所有方案（按名称索引） */
  schemes: Record<string, Scheme>;
  /** 当前激活的方案名 */
  activeScheme: string;
  /** 全局设置 */
  global: GlobalSettings;
}
```

### 4.2 智能体配置

```typescript
interface AgentConfig {
  /** 唯一标识（UUID，创建时生成） */
  id: string;
  /** 用户可见名称 */
  name: string;
  /** 备注 */
  note: string;
  /** 智能体标签名（用于标签系统匹配） */
  label: string;

  /** API 连接配置 */
  api: {
    /** API 地址 */
    url: string;
    /** API Key */
    apiKey: string;
    /** 模型名称 */
    model: string;
    /** API 格式（openai / claude / google 等） */
    format: string;
  };

  /**
   * 模型参数（键值对，由 api-params-map.json 驱动显示）
   * 包含所有 API 参数：
   * - 通用配置：stream, includeReasoning, reasoningEffort,
   *   max_tokens, temperature, top_p
   * - 小众配置：min_p, top_k, frequency_penalty, presence_penalty
   */
  params: Record<string, any>;

  /** 历史消息层数（用于聊天历史截取） */
  historyDepth: number;

  /** 后处理配置 */
  postProcess: PostProcessConfig;

  /** 重试策略配置 */
  retry: RetryConfig;
}
```

### 4.3 后处理配置

```typescript
interface PostProcessConfig {
  /** 写入方式 */
  writeMode: WriteMode;
  /** 追加模式前置换行 */
  appendNewline: boolean;
  /** 裁剪开始标签前 */
  trimBefore: {
    enabled: boolean;
    /** 开始标签文本 */
    tag: string;
  };
  /** 裁剪结束标签后 */
  trimAfter: {
    enabled: boolean;
    /** 结束标签文本 */
    tag: string;
  };
}
```

### 4.4 重试策略配置

```typescript
interface RetryConfig {
  /** 接收超时时长（秒），默认 300 */
  timeout: number;
  /** 最大重试次数，默认 3 */
  maxRetries: number;
  /** 重试时间间隔（秒），默认 1 */
  retryInterval: number;
  /** 完整性检查（结束标识符） */
  endMarker: {
    enabled: boolean;
    /** 结束标识符文本 */
    marker: string;
  };
  /** 变量解析联动检查 */
  varUpdateCheck: boolean;
}
```

### 4.5 方案

```typescript
interface Scheme {
  /** 方案名称 */
  name: string;
  /** 有序的智能体引用列表 */
  agents: SchemeAgentRef[];
}

interface SchemeAgentRef {
  /** 引用的智能体 ID */
  agentId: string;
  /** 是否启用 */
  enabled: boolean;
}
```

### 4.6 全局设置

```typescript
/**
 * Agents 全局设置
 * 持久化位置: 酒馆助手变量系统 global 层
 * 顶级键名: Agents_config
 */
interface GlobalSettings {
  /** 通知等级，默认 'notice' */
  notifyLevel: NotifyLevel;
  /** 全局标签策略，默认 'include' */
  globalPolicy: GlobalPolicy;
  /** 试生成提示词，默认 'please return [OJBK]' */
  testPrompt: string;
}
```

### 4.7 API 参数映射表

```typescript
/**
 * 存储于 data/api-params-map.json
 * 驱动 UI 参数控件的显示/隐藏 和 请求体构建时的参数筛选
 */
interface ApiParamsMap {
  [format: string]: ApiFormatParams;
}

interface ApiFormatParams {
  /** 通用参数（始终在 UI 显示） */
  common: string[];
  /** 小众参数（展开后在 UI 显示） */
  niche: string[];
  /** 各参数的默认值 */
  defaults: Record<string, any>;
}

/**
 * 初始映射表内容（可后续扩展）：
 *
 * openai:
 *   common: [stream, max_tokens, temperature, top_p]
 *   niche: [frequency_penalty, presence_penalty]
 *
 * claude:
 *   common: [stream, max_tokens, temperature, top_p]
 *   niche: [top_k]
 *
 * google:
 *   common: [stream, max_tokens, temperature, top_p]
 *   niche: [top_k]
 *
 * 未知格式 → 按「自定义」处理，显示全部参数
 */
```

### 4.8 默认值汇总

```typescript
/**
 * AgentConfig 默认值（创建新智能体时使用）
 */
const AGENT_DEFAULTS: Partial<AgentConfig> = {
  label: '',
  historyDepth: 10,
  params: {
    stream: true,
    includeReasoning: false,
    reasoningEffort: 'medium',
    max_tokens: 4096,
    temperature: 1.0,
    top_p: 1.0,
  },
  postProcess: {
    writeMode: 'assistant',
    appendNewline: true,
    trimBefore: { enabled: false, tag: '' },
    trimAfter: { enabled: false, tag: '' },
  },
  retry: {
    timeout: 300,
    maxRetries: 3,
    retryInterval: 1,
    endMarker: { enabled: false, marker: '' },
    varUpdateCheck: false,
  },
};

/**
 * GlobalSettings 默认值
 */
const GLOBAL_DEFAULTS: GlobalSettings = {
  notifyLevel: 'notice',
  globalPolicy: 'include',
  testPrompt: 'please return [OJBK]',
};
```
